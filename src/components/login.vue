<template>
</template>

<script>
import { XXX } from '../../api/XXX'     // 用来导入存放后端接口的js文件(花括号接收，多个接口之间用逗号隔开)，后面用axios请求后端接口
import XXX from '../../XXX'     // 用来导入其他组件
export default {
  name: '',     // 调试时用的名字
  data () {
    return {}   // 花括号里面用来放以key:value的方式存放我们定义的数据
  },
  components: {},   // 存放导入的组件名
  created() {},     // 存放在模板渲染成html前调用的函数
  mounted() {},     // 存放在模板渲染成html后调用的函数
  methods: {},      // 在这里面编写函数
}
</script>

<style scoped>
</style>
